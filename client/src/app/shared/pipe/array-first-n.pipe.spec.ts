import {ArrayFirstNPipe} from './array-first-n.pipe';

describe('ArrayFirstNPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayFirstNPipe();
    expect(pipe).toBeTruthy();
  });
});
